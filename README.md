# Secure Software Project

## How to access csv
The csv files were too large to upload so I had to compress them 

1) Go to project root directory
2) Combine split into single zip
    ```bash
    zip -s 0 output_data.zip --out full_data.zip
    ```
3) Unzip the combined file:
    ```bash
    unzip full_data.zip
    ```
